name: CI

on:
    push:
        branches: 
            - main
            - develop
            - feature/CoreCmake
    pull_request:
        branches:
            - '!main'
            - '!develop'
jobs:
    build:
        uses: ./.github/workflows/build.yml
    test:
        needs: build
        uses: ./.github/workflows/test.yml